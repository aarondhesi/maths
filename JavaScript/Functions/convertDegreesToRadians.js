/**
 * Converts degrees to radians
 * @param {number} angle angle in degrees
 * @returns {number} angle in radians
 */

const convertDegreesToRadians = (angle) => angle * Math.PI / 180;

export { convertDegreesToRadians };