/**
 * Converts radians to degrees
 * @param {number} angle angle in radians
 * @returns {number} angle in degrees
 */

 const convertRadiansToDegrees = (angle) => angle * 180 / Math.PI;

 export { convertRadiansToDegrees };