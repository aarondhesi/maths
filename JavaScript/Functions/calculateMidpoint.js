/**
 * Calculates midpoint of num1 and num2
 * @param {Number} num1 
 * @param {Number} num2 
 * @returns {Number} midpoint of num1 and num2
 */

const calculateMidpoint = (num1, num2) => (num1 + num2) / 2;

export { calculateMidpoint };